# .NET
bin/
obj/
*.user
artifacts/

# EF / SQLite databases
*.db
*.db-shm
*.db-wal

# Node / frontend
node_modules/
web/dist/
web/.vite/

# Env / secrets
.env
*.local

# Local build helper + signing keystores (never commit these)
/turnly-build.ps1
*.jks
*.keystore

# Android build artifacts (distribute via GitHub Releases, not the repo)
/release/
*.apk
*.aab

# Local dev config — holds dev-only secrets (JWT signing key, VAPID keys); not committed.
# Copy appsettings.Development.json.example to appsettings.Development.json to set up.
src/Turnly.Api/appsettings.Development.json

# IDE
.vs/
.idea/
.vscode/

# OS
.DS_Store

# Claude
.claude/
CLAUDE.md
